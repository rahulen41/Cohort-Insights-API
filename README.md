# Cohort-Insights-API
Cohort Insights API designed using FastAPI, MongoDB, Redis and Docker. The purpose of the project is to accept documents, process their content asynchronously through a two-stage pipeline, and return generated insights such as summaries and tags. The main goal is to allow users to create and update documents, process their content asynchronously.
